{
  "name": "@conclave/test-web-join",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Standalone smoke test for the apps/web join flow — drives the running dev server, does not import or edit apps/web source.",
  "scripts": {
    "smoke": "node smoke.mjs"
  }
}
